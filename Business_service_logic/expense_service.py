from config.db import db
from datetime import datetime

def add_expense(user_id, amount, category, expense_tyoe):
    """add a new expense under a single category"""

    # Rule 8


def change_expense_category(user_id, expense_id, new_category):
    """enforce rule 9 here"""
    return
